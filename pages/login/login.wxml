<!-- pages/login/login.wxml -->
<view class="container">
  <!-- 头部 -->
  <view class="header">
    <view class="logo">
      <van-icon name="service-o" size="64" color="#409eff" />
    </view>
    <text class="title">陪诊服务平台</text>
  </view>

  <!-- 登录表单 -->
  <view class="form-section">
    <!-- 手机号输入 -->
    <view class="input-group">
      <view class="input-wrapper">
        <text class="label-text">手机号码</text>
        <input 
          class="input-field" 
          placeholder="请输入手机号" 
          type="number" 
          maxlength="11"
          value="{{phone}}"
          bindinput="onPhoneInput"
        />
        <van-icon name="phone-o" size="20" color="#409eff" class="input-icon" />
      </view>
    </view>

    <!-- 密码输入 -->
    <view class="input-group">
      <view class="input-wrapper">
        <text class="label-text">密码</text>
        <input 
          class="input-field" 
          placeholder="请输入密码" 
          password="{{!showPassword}}"
          value="{{password}}"
          bindinput="onPasswordInput"
        />
        <van-icon name="eye-o" size="20" color="{{showPassword ? '#409eff' : '#999'}}" class="input-icon" bindtap="togglePassword" />
      </view>
      <text class="forgot-pwd" bindtap="goToForgotPassword">忘记密码?</text>
    </view>

    <!-- 登录按钮 -->
    <view class="login-btn {{canLogin ? '' : 'disabled'}}" bindtap="handleLogin">
      <text>登录</text>
    </view>

    <!-- 注册链接 -->
    <view class="register-link">
      <text>还没有账号？</text>
      <text class="link" bindtap="goToRegister">立即注册</text>
    </view>

    <!-- 协议 -->
    <view class="agreement">
      <checkbox class="agreement-checkbox" checked="{{agreed}}" bindtap="toggleAgreement" color="#409eff" />
      <view class="agreement-text" bindtap="handleAgreementClick">
        <text>我已阅读并同意</text>
        <text class="link">《用户协议》</text>
        <text>和</text>
        <text class="link">《隐私政策》</text>
      </view>
    </view>
  </view>
</view>